<h1>Articles</h1>

Le blog a <%= Article.public_count %> articles non archivés.

<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>
                <a href="<%= article_path(article) %>">
                    <%= article.title %>
                </a>
            </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "Créer un article", new_article_path %>